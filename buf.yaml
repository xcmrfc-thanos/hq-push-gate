version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # 契约文档（docs/04）冻结了枚举命名与编号，保持文档一致优先于 buf 默认命名风格
    - ENUM_VALUE_PREFIX
    - ENUM_ZERO_VALUE_SUFFIX
breaking:
  use:
    - FILE
